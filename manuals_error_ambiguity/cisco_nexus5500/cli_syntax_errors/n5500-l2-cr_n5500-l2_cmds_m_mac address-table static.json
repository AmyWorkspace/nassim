{
    "PageTitle": "mac address-table static",
    "FuncDef": "To configure a static entry for the MAC address table, use the mac address-table static command. To delete the static entry, use the no form of this command.",
    "CLIs": [
        "mac address-table static <mac-address> vlan <vlan-id> { drop | interface { ethernet <slot> /[ <QSFP-module> /] <port> | port-channel <number> [ . <subinterface-number> ]} [ auto-learn ]",
        "no mac address-table static <mac-address> { vlan <vlan-id> }"
    ],
    "ParentView": [
        "global configuration mode"
    ],
    "ParaDef": [
        {
            "Parameters": "mac-address",
            "Info": "MAC address to add to the table. Use the format EEEE.EEEE.EEEE."
        },
        {
            "Parameters": "vlan vlan-id",
            "Info": "Specifies the VLAN to apply the static MAC address. The VLAN ID range is from 1 to 4094."
        },
        {
            "Parameters": "drop",
            "Info": "Drops all traffic that is received from and going to the configured MAC address in the specified VLAN."
        },
        {
            "Parameters": "interface",
            "Info": "Specifies the interface. T he type can be either Ethernet or EtherChannel."
        },
        {
            "Parameters": "ethernet slot /[ QSFP-module /] port",
            "Info": "Specifies the Ethernet interface and the slot number and port number. The slot number is from 1 to 255. The QSFP-module number is from 1 to 4. The port number is from 1 to 128. Note The QSFP-module number applies only to the QSFP+ Generic Expansion Module (GEM)."
        },
        {
            "Parameters": "port-channel number",
            "Info": "Specifies the EtherChannel interface and EtherChannel number. The range is from 1 to 4096."
        },
        {
            "Parameters": ". subinterface-number",
            "Info": "(Optional) EtherChannel number followed by a dot (.) indicator and the subinterface number."
        },
        {
            "Parameters": "auto-learn",
            "Info": "(Optional) Allows the switch to automatically update this MAC address."
        }
    ],
    "Examples": [
        [
            "switch(config)# mac address-table static 0050.3e8d.6400 vlan 3 interface ethernet 1/4"
        ]
    ],
    "ExtraInfo": "You cannot apply the mac address-table static mac-address vlan vlan-id drop command to a multicast MAC address.When you install a static MAC address, it is associated with a port. If the same MAC address is seen on a different port, the entry is updated with the new port if you enter the auto-learn keyword.\nCommand Default:None",
    "HTMLPage": "data/manuals/cisco/cisco_nexus5500/n5500-l2-cr_n5500-l2_cmds_m.html",
    "ori_path": "data/cmdcorpus/cisco/cisco_nexus5500/cmd_corpus/n5500-l2-cr_n5500-l2_cmds_m/n5500-l2-cr_n5500-l2_cmds_m_mac address-table static.json",
    "msg": [
        "Error CLI:mac address-table static <mac-address> vlan <vlan-id> { drop | interface { ethernet <slot> /[ <QSFP-module> /] <port> | port-channel <number> [ . <subinterface-number> ]} [ auto-learn ]"
    ]
}