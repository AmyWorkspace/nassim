{
    "PageTitle": "monitor session",
    "FuncDef": "To create a new Ethernet Switched Port Analyzer (SPAN) or an Encapsulated Remote Switched Port Analyzer (ERSPAN) session configuration for analyzing traffic between ports or add to an existing session configuration, use the monitor session command. To clear SPAN or ERSPAN sessions, use the no form of this command.",
    "CLIs": [
        "monitor session { <session-number> [ shut | type { local | erspan-source } | all shut }",
        "no monitor session { <session-number> | all } [ shut ]"
    ],
    "ParentView": [
        "global configuration mode"
    ],
    "ParaDef": [
        {
            "Parameters": "session-number",
            "Info": "SPAN session to create or configure. The range is from 1 to 18."
        },
        {
            "Parameters": "all",
            "Info": "Specifies to apply configuration information to all SPAN sessions."
        },
        {
            "Parameters": "shut",
            "Info": "(Optional) Specifies that the selected session will be shut down for monitoring."
        },
        {
            "Parameters": "type",
            "Info": "(Optional) Specifies the type of session to configure."
        },
        {
            "Parameters": "local",
            "Info": "Specifies the session type to be local."
        },
        {
            "Parameters": "erspan-source",
            "Info": "Creates an ERSPAN source session."
        }
    ],
    "Examples": [
        [
            "switch# configure terminal",
            "switch(config)# monitor session 2",
            "switch(config)#"
        ],
        [
            "switch(config)# monitor session 9 type local",
            "switch(config-monitor)# description A Local SPAN session",
            "switch(config-monitor)# source interface ethernet 1/1",
            "switch(config-monitor)# destination interface ethernet 1/2",
            "switch(config-monitor)# no shutdown"
        ],
        [
            "switch(config)# interface ethernet 1/2",
            "switch(config-if)# switchport",
            "switch(config-if)# switchport monitor",
            "switch(config-if)# no shutdown"
        ],
        [
            "switch(config)# interface Ethernet1/2",
            "switch(config-if)# switchport",
            "switch(config-if)# switchport mode trunk",
            "switch(config-if)# switchport monitor",
            "switch(config-if)# switchport trunk allowed vlan 10-12",
            "switch(config-if)# no shutdown"
        ],
        [
            "switch# configure terminal",
            "switch(config)# monitor session 1 type erspan-source",
            "switch(config-erspan-src)#"
        ]
    ],
    "ExtraInfo": "To ensure that you are working with a completely new session, you can clear the desired session number or all SPAN sessions.Note The Cisco Nexus 5500 switch supports two active SPAN sessions. The Cisco Nexus 5548 Switch supports four active SPAN sessions. When you configure more than two SPAN sessions, the first two sessions are active. During startup, the order of active sessions is reversed; the last two sessions are active. For example, if you configured ten sessions 1 to 10 where 1 and 2 are active, after a reboot, sessions 9 and 10 will be active. To enable deterministic behavior, explicitly suspend the sessions 3 to 10 with the monitor session session-number shut command.After you create an ERSPAN session, you can describe the session and add interfaces and VLANs as sources and destinations.\nCommand Default:None",
    "HTMLPage": "data/manuals/cisco/cisco_nexus5500/n5500-sysmgmt-cr_n5500-sm_cmds_m.html",
    "ori_path": "data/cmdcorpus/cisco/cisco_nexus5500/cmd_corpus/n5500-sysmgmt-cr_n5500-sm_cmds_m/n5500-sysmgmt-cr_n5500-sm_cmds_m_monitor session.json",
    "msg": [
        "Error CLI:monitor session { <session-number> [ shut | type { local | erspan-source } | all shut }"
    ]
}