{
    "PageTitle": "flow control (Port extension interface view)",
    "FuncDef": "The flow control command enables flow control on an interface.\nThe undo flow control command disables flow control on an interface.\nThe flow control default command restores the flow control to the default.\nIf the port is on the CR53LPUF50A board, the upstream and downstream transmission functions of the traffic control function are disabled. If the port is on another board, the upstream receiving and downstream sending functions of the traffic control are enabled.\nBy default, the configuration of the interface is flow control default.",
    "CLIs": [
        "flow control { receive | send }",
        "flow control",
        "flow control default",
        "undo flow control"
    ],
    "ParentView": [
        "Port extension interface view"
    ],
    "ParaDef": [
        {
            "Parameters": "receive",
            "Info": "When the receive is enabled, the interface can receive pause frames and respond to pause frames. When a pause frame is received, the interface stops sending traffic.\n-"
        },
        {
            "Parameters": "send",
            "Info": "When the send is enabled, a pause frame is sent to the interconnected interface when the interface is congested.\n-"
        },
        {
            "Parameters": "default",
            "Info": "Specifies the flow control to the default.\n-"
        }
    ],
    "Examples": [
        [
            "<HUAWEI> system-view",
            "[~HUAWEI] interface GigabitEthernet1025/1/0/0",
            "[~HUAWEI-GigabitEthernet1025/1/0/0] flow control send"
        ],
        [
            "<HUAWEI> system-view",
            "[~HUAWEI] interface GigabitEthernet1025/1/0/0",
            "[~HUAWEI-GigabitEthernet1025/1/0/0] flow control receive"
        ]
    ],
    "ExtraInfo": "Usage Scenario\nEnsure that both devices at two ends of a link support flow control."
}