{
    "PageTitle": "flow control",
    "FuncDef": "The flow control command enables flow control on an interface.\nThe flow control default command restores the flow control to the default.\nThe undo flow control command disables flow control on an interface.\nBy default, send and receive flow control is disabled on the CR5DLPUF507L, CR5DLPUF5071, CR5DLPUF507M, CR5DLPUF5070 and enabled on all other boards.",
    "CLIs": [
        "flow control { receive | send }",
        "flow control",
        "flow control default",
        "undo flow control"
    ],
    "ParentView": [
        "100GE interface view",
        "10G LAN interface view",
        "10G WAN interface view",
        "25GE interface view",
        "400GE interface view",
        "40GE interface view",
        "50GE interface view",
        "50|100GE interface view",
        "FlexE interface view",
        "GE optical interface view",
        "GE electrical interface view"
    ],
    "ParaDef": [
        {
            "Parameters": "receive",
            "Info": "When the receive is enabled, the interface can receive pause frames and respond to pause frames. When a pause frame is received, the interface stops sending traffic.\n-"
        },
        {
            "Parameters": "send",
            "Info": "When the send is enabled, a pause frame is sent to the interconnected interface when the interface is congested.\n-"
        },
        {
            "Parameters": "default",
            "Info": "Specifies the flow control to the default.\n-"
        }
    ],
    "Examples": [
        [
            "<HUAWEI> system-view",
            "[~HUAWEI] interface GigabitEthernet 1/0/1",
            "[~HUAWEI-GigabitEthernet1/0/1] flow control receive"
        ],
        [
            "<HUAWEI> system-view",
            "[~HUAWEI] interface GigabitEthernet 1/0/1",
            "[~HUAWEI-GigabitEthernet1/0/1] flow control send"
        ]
    ],
    "ExtraInfo": "Usage Scenario\nEnsure that both devices at two ends of a link support flow control.\nPrecautions\nIf the peer device does not support flow control, disabling flow control on the local device is recommended."
}