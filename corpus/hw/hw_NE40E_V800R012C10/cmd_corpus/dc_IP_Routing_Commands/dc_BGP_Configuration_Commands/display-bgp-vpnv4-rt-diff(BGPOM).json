{
    "PageTitle": "display bgp routing-table different-origin-as",
    "FuncDef": "The display bgp vpnv4 routing-table different-origin-as command displays information about routes with the same destination address&mask but originating from different origin ASs.\nThe display bgp labeled routing-table different-origin-as command displays information about routes with the same destination address&mask but originating from different origin ASs in a labeled-unicast sub-address family.",
    "CLIs": [
        "display bgp vpnv4 { all | vpn-instance <vpn-instance-name> } routing-table different-origin-as",
        "display bgp instance <instance-name> vpnv4 { all | vpn-instance <vpn-instance-name> } routing-table different-origin-as",
        "display bgp labeled vpn-instance <vpn-instance-name> routing-table different-origin-as"
    ],
    "ParentView": [
        "All views"
    ],
    "ParaDef": [
        {
            "Parameters": "all",
            "Info": "Displays all BGP VPNv4 routes.\n-"
        },
        {
            "Parameters": "vpn-instance vpn-instance-name",
            "Info": "Specifies the name of a VPN instance.\nThe value is a string of 1 to 31 case-sensitive characters, spaces not supported. In addition, the VPN instance name must not be _public_. When double quotation marks are used around the string, spaces are allowed in the string."
        },
        {
            "Parameters": "different-origin-as",
            "Info": "Displays routes that have the same destination address but different source AS numbers.\n-"
        },
        {
            "Parameters": "instance instance-name",
            "Info": "Specifies a BGP multi-instance.\nThe value is a string of 1 to 31 case-sensitive characters, spaces not supported. When double quotation marks are used around the string, spaces are allowed in the string."
        }
    ],
    "Examples": [
        [
            "<HUAWEI> display bgp instance aaa vpnv4 all routing-table different-origin-as",
            " ",
            " Total number of routes from all PE: 0"
        ]
    ],
    "ExtraInfo": "Usage Scenario\nThe display bgp vpnv4 routing-table different-origin-as command displays information about routes with the same destination address&mask but originating from different origin ASs.\nThe display bgp labeled routing-table different-origin-as command displays information about routes with the same destination address&mask but originating from different origin ASs in a labeled-unicast sub-address family."
}