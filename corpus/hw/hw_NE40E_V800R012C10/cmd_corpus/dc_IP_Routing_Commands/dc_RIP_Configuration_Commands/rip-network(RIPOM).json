{
    "PageTitle": "network (RIP view)",
    "FuncDef": "The network command enables RIP for a specified network segment on an interface.\nThe undo network command disables RIP from a specified network segment on an interface.\nBy default, RIP is disabled for the specified network segment on an interface.",
    "CLIs": [
        "network <network-address>",
        "undo network <network-address>"
    ],
    "ParentView": [
        "RIP view"
    ],
    "ParaDef": [
        {
            "Parameters": "network-address",
            "Info": "Specifies the address of the network segment for which RIP is enabled.\nThe value must be the address of a natural network segment, in dotted decimal notation."
        }
    ],
    "Examples": [
        [
            "<HUAWEI> system-view",
            "[~HUAWEI] rip 1",
            "[*HUAWEI-rip-1] network 10.0.0.0"
        ]
    ],
    "ExtraInfo": "Usage Scenario\nThe network command enables RIP for a specified network segment on an interface.\nPrerequisites\nAn IP has been configured for the interface whose network segment is to be enabled with RIP.\nProcedure\nIn RIP, the network segments of the same physical interface must be specified for the same RIP process.\nImplementation Procedure\nIn RIP, the network segments of the same physical interface must be specified for the same RIP process."
}