{
    "PageTitle": "refresh bgp multicast",
    "FuncDef": "The refresh bgp multicast command softly resets an MBGP connection. MBGP soft resetting can be used to refresh the MBGP routing table and apply new routing policies, without closing any MBGP connection.",
    "CLIs": [
        "refresh bgp multicast { <peer-address> | all | external | internal | group <group-name> } { export | import }"
    ],
    "ParentView": [
        "User view"
    ],
    "ParaDef": [
        {
            "Parameters": "peer-address",
            "Info": "Softly resets the MBGP connections with a specified peer.\nThe peer address is in dotted decimal notation."
        },
        {
            "Parameters": "all",
            "Info": "Softly resets all MBGP connections.\n-"
        },
        {
            "Parameters": "external",
            "Info": "Indicates the external session.\n-"
        },
        {
            "Parameters": "internal",
            "Info": "Indicates the internal session.\n-"
        },
        {
            "Parameters": "group group-name",
            "Info": "Specifies the name of an MBGP peer group. If the parameter is set, the MBGP connections between a device and the members of the specified MBGP peer group are softly reset.\nThe value is a string of 1 to 47 case-sensitive characters, spaces not supported."
        },
        {
            "Parameters": "export",
            "Info": "Triggers outbound soft resetting.\n-"
        },
        {
            "Parameters": "import",
            "Info": "Triggers inbound soft resetting.\n-"
        }
    ],
    "Examples": [
        [
            "<HUAWEI> refresh bgp multicast all import"
        ],
        [
            "<HUAWEI> refresh bgp multicast external import"
        ],
        [
            "<HUAWEI> refresh bgp multicast internal import"
        ]
    ],
    "ExtraInfo": "Usage Scenario\nMBGP soft resetting requires that all MBGP devices on a network support the route-refresh capability. The refresh bgp multicast command can be used on a device that does not support the route-refresh capability to reset the connections between the device and its peer and enable the device to refresh its routing table."
}