{
    "PageTitle": "destination (Tunnel interface view)",
    "FuncDef": "The destination command configures a destination address for a tunnel.\nThe undo destination command deletes the destination address of a tunnel.\nBy default, no destination address is configured for a tunnel.",
    "CLIs": [
        "destination <ip-address>",
        "undo destination"
    ],
    "ParentView": [
        "Tunnel interface view"
    ],
    "ParaDef": [
        {
            "Parameters": "ip-address",
            "Info": "Specifies the destination address of a tunnel.\nFor a GRE or IPv6 over IPv4 tunnel, the destination address is the IP address of the remote tunnel interface that actually receives packets.For an MPLS TE tunnel, the destination address is usually set to the LSR ID of the egress.\nThe value is in dotted decimal notation."
        }
    ],
    "Examples": [
        [
            "<HUAWEI> system-view",
            "[~HUAWEI] mpls",
            "[*HUAWEI-mpls] mpls te",
            "[*HUAWEI-mpls] quit",
            "[*HUAWEI] interface Tunnel 10",
            "[*HUAWEI-Tunnel10] tunnel-protocol mpls te",
            "[*HUAWEI-Tunnel10] destination 4.4.4.4"
        ],
        [
            "<HUAWEI> system-view",
            "[~HUAWEI] interface Tunnel 10",
            "[*HUAWEI-Tunnel10] tunnel-protocol gre",
            "[*HUAWEI-Tunnel10] destination 2.2.2.2"
        ]
    ],
    "ExtraInfo": "Usage Scenario\nWhen configuring the tunnels, you need to create tunnel interfaces and use the destination command to configure the destination addresses of tunnels.\nPrerequisites\nThe tunnel mode has been configured using the tunnel-protocol command."
}