{
    "PageTitle": "root-ip (EVI-IPMSI-ROOT-MLDP view)",
    "FuncDef": "The root-ip command configures an IP address for the root node of an mLDP P2MP tunnel.\nThe undo root-ip command deletes the IP address configured for the root node of an mLDP P2MP tunnel.\nBy default, no IP address is configured for the root node of an mLDP P2MP tunnel.",
    "CLIs": [
        "root-ip <ip-address>",
        "undo root-ip"
    ],
    "ParentView": [
        "EVI-IPMSI-ROOT-MLDP view"
    ],
    "ParaDef": [
        {
            "Parameters": "ip-address",
            "Info": "Specifies an IP address for the root node of an mLDP P2MP tunnel.\nThe value is in dotted decimal notation."
        }
    ],
    "Examples": [
        [
            "<HUAWEI> system-view",
            "[~HUAWEI] evpn vpn-instance evpn1 bd-mode",
            "[*HUAWEI-evpn-instance-evpn1] inclusive-provider-tunnel",
            "[*HUAWEI-evpn-instance-evpn1-inclusive] root",
            "[*HUAWEI-evpn-instance-evpn1-inclusive-root] mldp p2mp",
            "[*HUAWEI-evpn-instance-evpn1-inclusive-root-mldpp2mp] root-ip 2.2.2.2"
        ]
    ],
    "ExtraInfo": "Usage Scenario\nAfter the root node is specified for an mLDP P2MP tunnel on a multicast EVPN network, run the root-ip command to configure an IP address for the root node. This IP address can be used as the destination address of leaf nodes so that the root node and leaf nodes can communicate.\nPrecautions\nIf the root-ip command is run more than once, the latest configuration overrides the previous one."
}