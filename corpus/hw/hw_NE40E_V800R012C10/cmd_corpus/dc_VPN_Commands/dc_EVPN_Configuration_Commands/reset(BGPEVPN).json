{
    "PageTitle": "reset bgp evpn",
    "FuncDef": "The reset bgp evpn command resets a specified or all BGP EVPN connections.",
    "CLIs": [
        "reset bgp evpn { all | <as-number> | <ipv4-address> | group <group-name> }"
    ],
    "ParentView": [
        "User view"
    ],
    "ParaDef": [
        {
            "Parameters": "all",
            "Info": "Resets all BGP EVPN connections.\n-"
        },
        {
            "Parameters": "as-number",
            "Info": "Specifies an AS number in dotted notation.\nThe value is in the format of x.y, where x and y are integers ranging from 1 to 65535 and from 0 to 65535, respectively."
        },
        {
            "Parameters": "ipv4-address",
            "Info": "Specifies a BGP EVPN peer IP address.\nThe value is in dotted decimal notation."
        },
        {
            "Parameters": "group group-name",
            "Info": "Resets BGP connections of the specified peer group.\nThe name is a string of 1 to 47 case-sensitive characters, with spaces not supported. When double quotation marks are used around the string, spaces are allowed in the string."
        }
    ],
    "Examples": [
        [
            "<HUAWEI> reset bgp evpn all"
        ]
    ],
    "ExtraInfo": "Usage Scenario\nTo reset all BGP EVPN connections, run the reset bgp evpn all command.\nConfiguration Impact\nThis command resets all TCP connections established between BGP EVPN peers and therefore results in the re-establishment of BGP EVPN peer relationships. Exercise caution when running this command."
}