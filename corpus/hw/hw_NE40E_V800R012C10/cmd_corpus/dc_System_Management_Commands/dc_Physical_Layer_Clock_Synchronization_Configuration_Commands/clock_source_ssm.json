{
    "PageTitle": "clock source ssm (system view)",
    "FuncDef": "The clock source ssm command configures an SSM level for a clock source.\nThe undo clock source ssm command restores the SSM level for a clock source.\nBy default (not configured and not learned), the SSM level is dnu.",
    "CLIs": [
        "clock source bits0 ssm { prc | ssua | ssub | sec | dnu | unk } [ slot <slotid> ]",
        "clock source [ chassis <chassis-id> ] { bits0 | bits1 | bits2 } ssm { prc | ssua | ssub | sec | dnu | unk }",
        "clock source ptp ssm { prc | ssua | ssub | sec | dnu | unk }",
        "clock source [ chassis <chassis-id> ] bits0 ssm { prc | ssua | ssub | sec | dnu | unk } [ slot <slotid> ]",
        "undo clock source bits0 ssm [ slot <slotid> ]",
        "undo clock source [ chassis <chassis-id> ] { bits0 | bits1 | bits2 } ssm",
        "undo clock source ptp ssm",
        "undo clock source [ chassis <chassis-id> ] bits0 ssm [ slot <slotid> ]"
    ],
    "ParentView": [
        "System view"
    ],
    "ParaDef": [
        {
            "Parameters": "prc",
            "Info": "Indicates that the SSM level is PRC (G.811 clock signal).\n-"
        },
        {
            "Parameters": "ssua",
            "Info": "Indicates that the SSM level is SSUA (G.812 transit node clock signal).\n-"
        },
        {
            "Parameters": "ssub",
            "Info": "Indicates that the SSM level is SSUB (G.812 local node clock signal).\n-"
        },
        {
            "Parameters": "sec",
            "Info": "Indicates that the SSM level is SEC.\n-"
        },
        {
            "Parameters": "dnu",
            "Info": "Indicates that the SSM level is not used for clock synchronization.\n-"
        },
        {
            "Parameters": "unk",
            "Info": "Indicates that the SSM level is unknown clock synchronization quality.\n-"
        },
        {
            "Parameters": "slot",
            "Info": "Specifies the slot ID of the main control board.\n-"
        },
        {
            "Parameters": "slotid",
            "Info": "Specifies the slot ID of the main control board.\nThe value is a string of 1 to 16 case-sensitive characters, spaces not supported."
        },
        {
            "Parameters": "chassis chassis-id",
            "Info": "chassis ID.\nThe value is an integer that ranges from 1 to 20."
        },
        {
            "Parameters": "bits0",
            "Info": "Specifies the bits clock source.\n-"
        },
        {
            "Parameters": "bits1",
            "Info": "Specifies the bits clock source.\n-"
        },
        {
            "Parameters": "bits2",
            "Info": "Specifies the bits clock source.\n-"
        },
        {
            "Parameters": "ptp",
            "Info": "Specifies the ptp clock source.\n-"
        }
    ],
    "Examples": [
        [
            "<HUAWEI> system-view",
            "[~HUAWEI] clock source ptp ssm prc"
        ]
    ],
    "ExtraInfo": "Usage Scenario\nA synchronization status message (SSM) transmits the quality level of timing signals on a synchronous timing link. Node clocks on a synchronization network parse the SSM to obtain the quality level of an upstream clock.\nIf SSM levels are configured to participate in clock source selection, the device selects a clock source to be traced based on the SSM levels of clock sources. To enable the clock sources to participate in clock source selection, configure SSM levels for the clock sources. 2 Mbps BITS clock signals have carried SSM levels, and therefore you do not need to configure SSM levels for 2 Mbps BITS clock sources.\nPriorities of SSM levels: prc > ssua > ssub > sec > dnu"
}