{
    "PageTitle": "terminal mask",
    "FuncDef": "The terminal mask name command enables a command line interface (CLI) terminal to filter out alarms based on the alarm filtering table.\nThe undo terminal mask command disables the CLI terminal from filtering out alarms based on the alarm filtering table.",
    "CLIs": [
        "terminal mask name <mask-name>",
        "undo terminal mask"
    ],
    "ParentView": [
        "Alarm management view"
    ],
    "ParaDef": [
        {
            "Parameters": "name mask-name",
            "Info": "Specifies the name of an alarm filtering table. The alarm filtering table must exist.\nThe value is a string of 1 to 31 characters, spaces not supported.\nWhen quotation marks are used around the string, spaces are allowed in the string."
        }
    ],
    "Examples": [
        [
            "<HUAWEI> system-view",
            "[~HUAWEI] alarm",
            "[~HUAWEI-alarm] mask name mask1",
            "[*HUAWEI-alarm-mask-mask1] commit",
            "[~HUAWEI-alarm-mask-mask1] quit",
            "[~HUAWEI-alarm] terminal mask name mask1"
        ]
    ],
    "ExtraInfo": "Usage Scenario\nAn alarm filtering table can be bound to a CLI terminal to filter out alarms that users are not concerned about.\nPrerequisites\nThe alarm filtering table exists."
}