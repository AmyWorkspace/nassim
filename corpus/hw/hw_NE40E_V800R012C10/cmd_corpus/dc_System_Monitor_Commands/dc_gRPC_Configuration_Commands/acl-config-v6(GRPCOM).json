{
    "PageTitle": "acl (GRPC IPv6 server view)",
    "FuncDef": "The acl command configures an ACL for the gRPC IPv6 service during Telemetry dynamic subscription.\nThe undo acl command deletes an ACL of the gRPC IPv6 service during Telemetry dynamic subscription.\nBy default, no ACL is configured for the gRPC IPv6 service during Telemetry dynamic subscription.",
    "CLIs": [
        "acl <acl-name>",
        "acl <acl-number>",
        "undo acl [ <acl-name> ]",
        "undo acl <acl-number>"
    ],
    "ParentView": [
        "GRPC IPv6 server view"
    ],
    "ParaDef": [
        {
            "Parameters": "acl-name",
            "Info": "Creates a named IPv6 ACL.\nThe value is a string of 1 to 64 case-sensitive characters, spaces not supported. The name must start with a letter or digit, and cannot contain only digits."
        },
        {
            "Parameters": "acl-number",
            "Info": "Creates a basic or numbered advanced IPv6 ACL.\nThe value is an integer ranging from 2000 to 3999.\nThe number of a basic ACL ranges from 2000 to 2999.The number of an advanced ACL ranges from 3000 to 3999."
        }
    ],
    "Examples": [
        [
            "<HUAWEI> system-view",
            "[~HUAWEI] acl ipv6 2000",
            "[*HUAWEI-acl6-basic-2000] quit",
            "[*HUAWEI] grpc",
            "[*HUAWEI-grpc] grpc server ipv6",
            "[*HUAWEI-grpc-server-ipv6] acl 2000"
        ],
        [
            "<HUAWEI> system-view",
            "[~HUAWEI] acl ipv6 name abc",
            "[*HUAWEI-acl6-advance-abc] quit",
            "[*HUAWEI] grpc",
            "[*HUAWEI-grpc] grpc server ipv6",
            "[*HUAWEI-grpc-server-ipv6] acl abc"
        ]
    ],
    "ExtraInfo": "Usage Scenario\nIf a collector functioning as a client initiates a connection to a device functioning as an IPv6 server, the data sampled is dynamically subscribed to. In this case, you can run the acl command to configure an ACL for the gRPC IPv6 service. This allows certain clients to connect to the server.\nPrerequisites\nAn IPv6 ACL has been created."
}