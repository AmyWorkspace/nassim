{
    "PageTitle": "sflow flow-sampling rate",
    "FuncDef": "The sflow flow-sampling rate command configures a flow sampling rate on an interface.\nThe undo sflow flow-sampling rate command restores the default flow sampling rate on an interface.\nBy default, the sampling rate is 8192.",
    "CLIs": [
        "sflow flow-sampling rate <rate> { inbound | outbound }",
        "undo sflow flow-sampling rate <rate> { inbound | outbound }"
    ],
    "ParentView": [
        "100GE interface view",
        "25GE interface view",
        "400GE interface view",
        "40GE interface view",
        "50GE interface view",
        "50|100GE interface view",
        "GE interface view",
        "GE electrical interface view"
    ],
    "ParaDef": [
        {
            "Parameters": "rate rate",
            "Info": "Specifies a sampling rate in the format of 1/rate. rate specifies the number of packets out of which the interface will sample a packet.\nThe value is an integer ranging from 1 to 65535."
        },
        {
            "Parameters": "inbound",
            "Info": "Specifies to get inbound direction sampling packet.\n-"
        },
        {
            "Parameters": "outbound",
            "Info": "Specifies to get inbound direction sampling packet.\n-"
        }
    ],
    "Examples": [
        [
            "<HUAWEI> system-view",
            "[*HUAWEI] sflow",
            "[~HUAWEI-sflow] sflow collector 1",
            "[~HUAWEI-sflow-collector-1] quit",
            "[~HUAWEI-sflow] quit",
            "[~HUAWEI] interface GigabitEthernet1/0/1",
            "[*HUAWEI-GigabitEthernet1/0/1] sflow flow-sampling collector 1 inbound",
            "[*HUAWEI-GigabitEthernet1/0/1] sflow flow-sampling rate 1000 inbound"
        ]
    ],
    "ExtraInfo": "Usage Scenario\nTo configure a flow sampling rate on an interface, run the sflow flow-sampling rate command. Setting a large sampling rate reduces the system's load but distorts the sampled data more than when a small sampling rate is set."
}