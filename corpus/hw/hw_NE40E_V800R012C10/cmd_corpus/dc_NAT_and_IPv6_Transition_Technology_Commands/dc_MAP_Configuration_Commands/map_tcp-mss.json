{
    "PageTitle": "map tcp adjust-mss",
    "FuncDef": "The map tcp adjust-mss command sets an MSS value used in the TCP three-way handshake.\nThe undo map tcp adjust-mss command restores the default MSS value used in the TCP three-way handshake.\nBy default, the MSS value used in the TCP three-way handshake is not modified.",
    "CLIs": [
        "map tcp adjust-mss <mss-value>",
        "undo map tcp adjust-mss [ <mss-value> ]"
    ],
    "ParentView": [
        "MAP-E instance view",
        "MAP-T instance view"
    ],
    "ParaDef": [
        {
            "Parameters": "mss-value",
            "Info": "Specifies the maximum length of packets to be sent without being fragmented.\nThe value is an integer ranging from 160 to 9540, in bytes."
        }
    ],
    "Examples": [
        [
            "<HUAWEI> system-view",
            "[~HUAWEI] map-t instance test id 111",
            "[*HUAWEI-mapt-instance-test] map tcp adjust-mss 1400"
        ]
    ],
    "ExtraInfo": "To set the TCP MSS value, run the map tcp adjust-mss command in a MAP instance view. After this command is run, the MAP-BR compares the configured MSS and the MSS field carried in a received TCP three-way handshake packet and selects a smaller one as an effective MSS value."
}