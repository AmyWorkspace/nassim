{
    "PageTitle": "crl ldap",
    "FuncDef": "The crl ldap command configures the automatic update of the CRL through LDAP.\nThe undo crl ldap command cancels the automatic update of the CRL through LDAP.\nBy default, the CRL is updated automatically through HTTP.",
    "CLIs": [
        "crl ldap [ attribute <attr-value> ] dn <dn-value>",
        "undo crl ldap [ attribute <attr-value> ] dn <dn-value>"
    ],
    "ParentView": [
        "PKI domain name configuration view"
    ],
    "ParaDef": [
        {
            "Parameters": "attribute attr-value",
            "Info": "Specifies the attribute value that the device uses when obtaining the CRL from the LDAP server.\nIt is a string of 1 to 63 characters and case sensitive."
        },
        {
            "Parameters": "dn dn-value",
            "Info": "Specifies the ID that the device uses when obtaining the CRL from the LDAP server. The ID is generally composed of the information such as the user common name, organization name, country, or the name of the certificate holder.\nIt is a string of 1 to 255 characters in text format and spaces are valid and case sensitive."
        }
    ],
    "Examples": [
        [
            "<HUAWEI> system-view",
            "[~HUAWEI] pki domain domain1",
            "[*HUAWEI-pki-domain-domain1] crl ldap dn huawei1"
        ]
    ],
    "ExtraInfo": "Usage Scenario\nTo use LDAP for updating the CRL automatically, run the crl ldap command first.\nYou should first run the crl ldap command to configure the automatic update of the CRL through LDAP, and then run the crl ldap dn command.\nNote the following when the device automatically updates the CRL through LDAP:\nThe CRL file cannot exceed 1 MB.There is sufficient space in the CF card for the CRL file."
}