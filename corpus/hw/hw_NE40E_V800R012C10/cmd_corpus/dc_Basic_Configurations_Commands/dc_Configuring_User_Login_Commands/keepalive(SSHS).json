{
    "PageTitle": "ssh server keepalive enable",
    "FuncDef": "The ssh server keepalive enable command enables the keepalive feature on an SSH server.\nThe undo ssh server keepalive enable command disables the keepalive feature on the SSH server.\nBy default, the keepalive feature is enabled on the SSH server.",
    "CLIs": [
        "ssh server keepalive enable",
        "undo ssh server keepalive enable"
    ],
    "ParentView": [
        "System view"
    ],
    "ParaDef": null,
    "Examples": [
        [
            "<HUAWEI> system-view",
            "[~HUAWEI] ssh server keepalive enable"
        ]
    ],
    "ExtraInfo": "Usage Scenario\nThe SSH server sends a keepalive response to the SSH client only if the keepalive feature is enabled on the SSH server. Otherwise the server discards the connection.\nThis command takes effect for both IPv4 and IPv6 connections."
}