{
    "PageTitle": "tftp-server acl",
    "FuncDef": "The tftp-server acl command enables the ACL of IPv4 configuration.\nThe undo tftp-server acl command cancels the ACL configuration.\nBy default, the ACL is not configured for the TFTP client.",
    "CLIs": [
        "tftp-server acl { <acl-number> | <acl-name> }",
        "undo tftp-server acl"
    ],
    "ParentView": [
        "System view"
    ],
    "ParaDef": [
        {
            "Parameters": "acl-number",
            "Info": "Specifies the ACL number.\nThe value is an integer ranging from 2000 to 2999."
        },
        {
            "Parameters": "acl-name",
            "Info": "Specifies the name of a named basic ACL.\nThe value is a string of 1 to 64 case-sensitive characters without spaces. The value must start with a letter (a to z or A to Z, case sensitive)."
        }
    ],
    "Examples": [
        [
            "<HUAWEI> system-view",
            "[~HUAWEI] acl 2000",
            "[*HUAWEI-acl4-basic-2000] quit",
            "[*HUAWEI] tftp-server acl 2000"
        ]
    ],
    "ExtraInfo": "Usage Scenario\nIf a device serves as a TFTP client, you can configure the ACL on the device to control the login of the local device to the TFTP server through TFTP.\nIf no rule is configured, the incoming and outgoing calls are not restricted after the command tftp-server acl is run."
}