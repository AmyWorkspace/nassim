{
    "PageTitle": "ftp ipv6 timeout",
    "FuncDef": "The ftp ipv6 timeout command sets the idle timeout interval of the client.\nThe undo ipv6 ftp timeout command restores the idle timeout interval to its default value.\nBy default, the timeout value is 10 minutes.",
    "CLIs": [
        "ftp ipv6 timeout <minutes>",
        "undo ftp ipv6 timeout"
    ],
    "ParentView": [
        "System view"
    ],
    "ParaDef": [
        {
            "Parameters": "minutes",
            "Info": "Specifies the timeout interval in minutes.\nTimeout interval is an integer data type. The value range is from 1 to 35791 minutes."
        }
    ],
    "Examples": [
        [
            "<HUAWEI> system-view",
            "[~HUAWEI] ftp ipv6 timeout 50"
        ]
    ],
    "ExtraInfo": "Usage Scenario\nThe timeout interval is set to disconnect the services provided by the FTP server to the client, which is idle for the specified time."
}