{
    "PageTitle": "replace configuration pattern",
    "FuncDef": "The replace configuration pattern command replaces the source character string with the target character string.",
    "CLIs": [
        "replace configuration pattern <src-string> with <target-string>"
    ],
    "ParentView": [
        "All views except the user view"
    ],
    "ParaDef": [
        {
            "Parameters": "src-string",
            "Info": "Specifies the source character string.\nThe value is a string of 1 to 32 case-sensitive characters, including letters, digits, hyphens (-), and underscores (_). Spaces are not supported."
        },
        {
            "Parameters": "with",
            "Info": "Replaces <expression> with <target-string>.\n-"
        },
        {
            "Parameters": "target-string",
            "Info": "Specifies the target character string.\nThe value is a string of 1 to 64 case-sensitive characters, including letters, digits, hyphens (-), and underscores (_). Spaces are not supported."
        }
    ],
    "Examples": [
        [
            "<HUAWEI> system-view",
            "[~HUAWEI] replace configuration pattern huawei with huawei123"
        ]
    ],
    "ExtraInfo": "Usage Scenario\nTo replace a character or a type of character on a device, run the replace configuration pattern command. The command supports batch replacement.\nFollow-up Procedure\nAfter character replacement is complete, the configuration file enters the candidate configuration database. Run the commit command for relevant configurations to take effect.\nPrecautions\nThis function is supported only in the two-phase configuration validation mode.\nThis function replaces only the character strings in the current view.\nThis command can only be used to replace a single command keyword or parameter."
}