{
    "PageTitle": "senderid-tlv-type",
    "FuncDef": "The senderid-tlv-type command configures the type of the sender ID TLV in a CFM packet.\nThe undo senderid-tlv-type command restores the default type of the sender ID TLV in a CFM packet.\nThe default type of the sender ID TLV is defer.",
    "CLIs": [
        "senderid-tlv-type { { chassis | chassis-manage } [ chassis-subtype { mac-address | locally-assigned } ] | defer | manage | none }",
        "undo senderid-tlv-type"
    ],
    "ParentView": [
        "MD view"
    ],
    "ParaDef": [
        {
            "Parameters": "chassis",
            "Info": "Indicates that a CFM packet contains the chassis ID.\n-"
        },
        {
            "Parameters": "chassis-manage",
            "Info": "Indicates that a CFM packet contains the chassis ID and management address.\n-"
        },
        {
            "Parameters": "chassis-subtype",
            "Info": "Indicates that a CFM packet contains the chassis ID subtype.\n-"
        },
        {
            "Parameters": "mac-address",
            "Info": "Indicates that a CFM packet contains the MAC address.\n-"
        },
        {
            "Parameters": "locally-assigned",
            "Info": "Indicates that a CFM packet contains the locally assigned.\n-"
        },
        {
            "Parameters": "defer",
            "Info": "Indicates that the content of the sender ID TLV is determined by the MD object.\n-"
        },
        {
            "Parameters": "manage",
            "Info": "Indicates that a CFM packet contains the management address.\n-"
        },
        {
            "Parameters": "none",
            "Info": "Indicates that a CFM packet does not contain the type of the sender ID TLV.\n-"
        }
    ],
    "Examples": [
        [
            "<HUAWEI> system-view",
            "[~HUAWEI] cfm enable",
            "[*HUAWEI] cfm md md1",
            "[*HUAWEI-md-md1] senderid-tlv-type manage"
        ]
    ],
    "ExtraInfo": "Usage Scenario\nThe senderid-tlv-type command can be used only on the device running IEEE Standard 802.1ag-2007.\nYou can run the senderid-tlv-type command to configure the type of the sender ID TLV in a CFM packet in the MD view.\nPrerequisites\nRun cfm md command to create an MD."
}