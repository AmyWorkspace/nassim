{
    "PageTitle": "remark ip-precedence",
    "FuncDef": "The remark ip-precedence command re-marks the precedence of IP packets.\nThe undo remark ip-precedence command cancels the configuration.\nBy default, the re-marking the precedence of IP packets is not configured in traffic behavior view.",
    "CLIs": [
        "remark ip-precedence <ip-precedence>",
        "undo remark ip-precedence [ ip-precedence ]"
    ],
    "ParentView": [
        "Traffic behavior view"
    ],
    "ParaDef": [
        {
            "Parameters": "ip-precedence",
            "Info": "Specifies the value of IP precedence.\nIt is an integer ranging from 0 to 7."
        }
    ],
    "Examples": [
        [
            "<HUAWEI> system-view",
            "[~HUAWEI] traffic behavior b1",
            "[*HUAWEI-behavior-b1] remark ip-precedence 1"
        ]
    ],
    "ExtraInfo": "Usage Scenario\nYou can run this command to re-mark the IP precedence of packets in the traffic classifier.\nPrerequisites\nThe traffic behavior is configured.\nFollow-up Procedure\nThe traffic policy is configured and the policy is applied to interfaces.\nPrecautions\nIf the remark ip-precedence command is run repeatedly in the same traffic behavior view, the later configuration overrides the previous one.\nThis command re-marks only the outermost label of packets."
}