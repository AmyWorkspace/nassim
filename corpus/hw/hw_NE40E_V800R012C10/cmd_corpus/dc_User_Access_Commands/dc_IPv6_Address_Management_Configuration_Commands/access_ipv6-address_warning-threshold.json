{
    "PageTitle": "access ipv6-address warning-threshold",
    "FuncDef": "The access ipv6-address warning-threshold command configures upper and lower IPv6 address alarm thresholds for a device.\nThe undo access ipv6-address warning-threshold command restores the default configuration.\nBy default, the IPv6 address usage threshold-crossing alarm is not enabled on a device.",
    "CLIs": [
        "access ipv6-address warning-threshold upper-limit <upper-limit-val> lower-limit <lower-limit-val>",
        "undo access ipv6-address warning-threshold { upper-limit | lower-limit }"
    ],
    "ParentView": [
        "System view"
    ],
    "ParaDef": [
        {
            "Parameters": "lower-limit lower-limit-val",
            "Info": "Specifies a lower alarm threshold.\nThe value is an integer ranging from 0 to 99."
        },
        {
            "Parameters": "upper-limit upper-limit-val",
            "Info": "Specifies an upper alarm threshold.\nThe value is an integer ranging from 1 to 100."
        }
    ],
    "Examples": [
        [
            "<HUAWEI> system-view",
            "[~HUAWEI] access ipv6-address warning-threshold upper-limit 80 lower-limit 20"
        ]
    ],
    "ExtraInfo": "Usage Scenario\nTo configure upper and lower IPv6 address alarm thresholds, run the access ipv6-address warning-threshold command. When the percentage of the number of IPv6 addresses of access users on a device in the device's IPv6 specification (1M) exceeds the upper alarm threshold or goes below the lower alarm threshold, the device generates an alarm.\nPrecautions\nIn VS mode, this command is supported only by the admin VS."
}