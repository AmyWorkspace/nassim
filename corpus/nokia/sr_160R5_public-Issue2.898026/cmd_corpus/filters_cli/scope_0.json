{
    "PageTitle": "scope",
    "FuncDef": "This command configures the filter policy scope as exclusive, template, embedded or system.",
    "CLIs": [
        "scope { exclusive | template | embedded | system }",
        "scope { exclusive | template }",
        "no scope"
    ],
    "ParentView": [
        "config>filter>ip-filter",
        "config>filter>ipv6-filter",
        "config>filter>mac-filter"
    ],
    "ParaDef": [
        {
            "Parameters": "exclusive",
            "Info": "Specifies that the policy can only be applied to a single entity. Attempting to assign the policy to a second entity will result in an error message."
        },
        {
            "Parameters": "template",
            "Info": "Specifies that the policy can be applied to multiple entities."
        },
        {
            "Parameters": "embedded",
            "Info": "Specifies that the policy cannot be applied directly. The policy defines embedded filter rules, which are embedded by other exclusive/template/system filter policies. The embedded scope is supported for IPv4 and IPv6 filter policies only."
        },
        {
            "Parameters": "system",
            "Info": "Specifies that the policy defines system-wide filter rules. To apply system policy rules, activate system filter and chain exclusive/template ACL filter policy to the system filter. The system scope is supported for IPv4 and IPv6 filter policies only."
        }
    ],
    "Examples": null,
    "ExtraInfo": "Default: scope template"
}