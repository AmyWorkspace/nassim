{
    "PageTitle": "src-ip",
    "FuncDef": "This command configures a source IPv4 or IPv6 address range to be used as an IP filter or IP exception match criterion.",
    "CLIs": [
        "src-ip { <ip-address/mask> | <ip-address> <ipv4-address-mask> | ip-prefix-list <prefix-list-name> }",
        "src-ip { <ipv6-address/prefix-length> | <ipv6-address> <ipv6-address-mask> | ipv6-prefix-list <prefix-list-name> }",
        "no src-ip"
    ],
    "ParentView": [
        "config>filter>ip-filter>entry>match",
        "config>filter>ipv6-filter>entry>match"
    ],
    "ParaDef": [
        {
            "Parameters": "ip-address",
            "Info": "Specifies the destination IPv4 address specified in dotted decimal notation. Values-a.b.c.d."
        },
        {
            "Parameters": "mask",
            "Info": "Specifies the length in bits of the subnet mask. Values-1 to 32."
        },
        {
            "Parameters": "ipv4-address-mask",
            "Info": "Specifies the subnet mask in dotted decimal notation. Values-a.b.c.d (dotted quad equivalent of mask length)."
        },
        {
            "Parameters": "ip-prefix-list or ipv6-prefix-list prefix-list-name",
            "Info": "Specifies to use a list of IP prefixes referred to by prefix-list-name, which is a string of up to 32 characters of printable ASCII characters. If special characters are used, the string must be enclosed within double quotes."
        },
        {
            "Parameters": "ipv6-address",
            "Info": "Specifies an IPv6 prefix for the IP match criterion in hex digits. Values-x:x:x:x:x:x:x:x (eight 16-bit pieces)."
        },
        {
            "Parameters": "prefix-length",
            "Info": "Specifies whether a the IPv6 prefix length for the specified ipv6-address expressed as a decimal integer. Values-1 to 128."
        },
        {
            "Parameters": "ipv6-address-mask",
            "Info": "Specifies eight 16-bit hexadecimal pieces representing bit match criteria. Values-x:x:x:x:x:x:x:x (eight 16-bit pieces)."
        }
    ],
    "Examples": null,
    "ExtraInfo": "Default: no src-ip"
}