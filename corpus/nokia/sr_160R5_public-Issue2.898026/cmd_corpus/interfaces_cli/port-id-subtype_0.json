{
    "PageTitle": "port-id-subtype",
    "FuncDef": "This command specifies how to encode the PortID TLV transmit to the peer. Some releases of the NSP NFM-P require the PortID value require the default if-Alias in order to properly build the Layer Two topology map using LLDP. Selecting a different option will impact the NSP NFM-P s ability to build those Layer Two topologies.",
    "CLIs": [
        "port-id-subtype { tx-if-alias | tx-if-name | tx-local }"
    ],
    "ParentView": [
        "config>port>ethernet>lldp>dstmac"
    ],
    "ParaDef": [
        {
            "Parameters": "tx-if-alias",
            "Info": "Transmits the ifAlias String (subtype 1) that describes the port as stored in the IF-MIB, either user configured or the default entry (i.e. 10/100/Gig Ethernet SFP)."
        },
        {
            "Parameters": "tx-if-name",
            "Info": "Transmits the ifName string (subtype 5) that describes the port as stored in the IF-MIB ifName info."
        },
        {
            "Parameters": "tx-local",
            "Info": "The interface ifIndex value (subtype 7) as the PortID."
        }
    ],
    "Examples": null,
    "ExtraInfo": "Default: port-id-subtype tx-local"
}