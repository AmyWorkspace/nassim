{
    "PageTitle": "filter",
    "FuncDef": "This command associates an IP filter policy or MAC filter policy on egress or ingress. Filter policies control the forwarding and dropping of packets based on IP or MAC matching criteria. There are two types of filter policies: IP and MAC. Only one type may be applied to a SAP at a time.",
    "CLIs": [
        "filter ip <ip-filter-id>",
        "filter ipv6 <ipv6-filter-id>",
        "filter mac <mac-filter-id>",
        "no filter [ ip <ip-filter-id> ] [ mac <mac-filter-id> ] [ ipv6 <ipv6-filter-id> ]"
    ],
    "ParentView": [
        "config>service>pw-template>egress",
        "config>service>pw-template>ingress"
    ],
    "ParaDef": [
        {
            "Parameters": "ip-filter-id",
            "Info": "Specifies the IP filter policy. The filter ID must already exist within the created IP filters. Values-1 to 65535."
        },
        {
            "Parameters": "ipv6-filter-id",
            "Info": "Specifies the IPv6 filter policy. The filter ID must already exist within the created IPv6 filters. Values-1 to 65535."
        },
        {
            "Parameters": "mac-filter-id",
            "Info": "Specifies the MAC filter policy. The specified filter ID must already exist within the created MAC filters. The filter policy must already exist within the created MAC filters. Values-1 to 65535."
        }
    ],
    "Examples": null,
    "ExtraInfo": ""
}