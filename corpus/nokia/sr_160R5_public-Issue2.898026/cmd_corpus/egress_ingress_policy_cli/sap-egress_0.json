{
    "PageTitle": "sap-egress",
    "FuncDef": "This command is used to create or edit a Service Egress QoS policy. The egress policy defines the SLA for service packets as they egress on the SAP.",
    "CLIs": [
        "sap-egress { <policy-id> | <policy-name> } [ create ] [ name <name> ]",
        "no sap-egress { <policy-id> | <policy-name> }"
    ],
    "ParentView": [
        "config>qos"
    ],
    "ParaDef": [
        {
            "Parameters": "policy-id",
            "Info": "The policy-id uniquely identifies the policy on the router. Values-1 to 65535."
        },
        {
            "Parameters": "policy-name",
            "Info": "The policy-name uniquely identifies the policy. Values-64 characters maximum.."
        },
        {
            "Parameters": "create",
            "Info": "Required parameter when creating a SAP QoS egress policy."
        },
        {
            "Parameters": "name",
            "Info": "Configures an optional policy name, up to 64 characters in length, which adds a name identifier to a given policy to then use that policy name in configuration references as well as display and use policy names in show commands throughout the system. This helps the service provider or administrator to identify and manage sap-egress policies within the SR OS platforms. Values-64 characters maximum."
        }
    ],
    "Examples": null,
    "ExtraInfo": ""
}