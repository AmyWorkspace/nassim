{
    "PageTitle": "burst-limit",
    "FuncDef": "The queueburst-limitcommand defines an explicit shaping burst size for a queue. The configured size defines the shaping leaky bucket threshold level that indicates the maximum burst over the queue's shaping rate.Thenoform of this command restores the default burst limit to the specified queue. This is equivalent to specifying burst-limit default within the QoS policies. When specified within a queue-override queue context, any current burst limit override for the queue is removed and the queue's burst limit is controlled by its defining policy.",
    "CLIs": [
        "burst-limit { default | <size> [ bytes | kilobytes ] }",
        "no burst-limit"
    ],
    "ParentView": [
        "config>service>vpls>sap>egress>queue-override>queue"
    ],
    "ParaDef": [
        {
            "Parameters": "default",
            "Info": "Reverts the queue's burst limit to the system default value."
        },
        {
            "Parameters": "size",
            "Info": "When a numeric value is specified (size), the system interprets the value as an explicit burst limit size. The value is expressed as an integer and by default is interpreted as the burst limit in kilobytes. If the value is intended to be interpreted in bytes, the bytes qualifier must be added following size. Values-1 to 13671 kilobytes. ParamDefault: Default- No default for size; use the default keyword to specify default burst limit."
        },
        {
            "Parameters": "bytes",
            "Info": "Specifies that the value given for size must be interpreted as the burst limit in bytes."
        },
        {
            "Parameters": "kilobytes",
            "Info": "Specifies that the value given for size must be interpreted as the burst limit in kilobytes. If neither bytes nor kilobytes is specified, the default qualifier is kilobytes."
        }
    ],
    "Examples": null,
    "ExtraInfo": "Default: no burst-limit"
}