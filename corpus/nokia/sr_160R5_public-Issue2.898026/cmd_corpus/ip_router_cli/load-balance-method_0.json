{
    "PageTitle": "load-balance-method",
    "FuncDef": "This command describes how new sessions are assigned to an L2TP ISA MDA.",
    "CLIs": [
        "load-balance-method { per-session | per-tunnel }",
        "no load-balance-method"
    ],
    "ParentView": [
        "config>router>l2tp>group",
        "config>router>l2tp>group>tunnel"
    ],
    "ParaDef": [
        {
            "Parameters": "per-session",
            "Info": "Specifies that the lowest granularity for load-balancing is a session; each session can be assigned to a different."
        },
        {
            "Parameters": "per-tunnel",
            "Info": "Specifies that the lowest granularity for load-balancing is a tunnel; all sessions associated with the same tunnel are assigned to the same ISA MDA; this may be useful or required in certain cases, for example:."
        }
    ],
    "Examples": null,
    "ExtraInfo": "Default: load-balance-method per-session"
}