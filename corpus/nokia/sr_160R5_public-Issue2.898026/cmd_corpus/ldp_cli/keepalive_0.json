{
    "PageTitle": "keepalive",
    "FuncDef": "This command configures the time interval (in s), that LDP waits before tearing down the session. Thefactorparameter derives the keepalive interval.",
    "CLIs": [
        "keepalive <timeout> <factor>",
        "no keepalive"
    ],
    "ParentView": [
        "config>router>ldp>if-params>if>ipv4",
        "config>router>ldp>if-params>if>ipv6",
        "config>router>ldp>if-params>ipv4",
        "config>router>ldp>if-params>ipv6",
        "config>router>ldp>targ-session>ipv4",
        "config>router>ldp>targ-session>ipv6",
        "config>router>ldp>targ-session>peer",
        "config>router>ldp>targ-session>peer-template"
    ],
    "ParaDef": [
        {
            "Parameters": "timeout",
            "Info": "Configures the time interval, expressed in seconds, that LDP waits before tearing down the session. Values-1 to 65535."
        },
        {
            "Parameters": "factor",
            "Info": "Specifies the number of keepalive messages, expressed as a decimal integer, that should be sent on an idle LDP session in the keepalive timeout interval. Values-1 to 255."
        }
    ],
    "Examples": null,
    "ExtraInfo": "Default: Table 61 lists the default values.\n\t\t\t\tTable 61: \n\t\t\tTimeout Factor Defaults ContextTimeoutFactor\nconfig>router>ldp>if-params303\nconfig>router>ldp>targ-session404\nconfig>router>ldp>if-params>ifInherits values from interface-parameters context.\nconfig>router>ldp>targ-session>peerInherits values from targeted-session context."
}